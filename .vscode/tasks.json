{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "sample_cmake",
            "type": "shell",
            "command": "docker exec docker_emscripten bash -c 'cd /opt/src/sample/build/ && cmake .. && make'"
        },
        {
            "label": "sample02_cmake",
            "type": "shell",
            "command": "docker exec docker_emscripten bash -c 'cd /opt/src/sample02/build/ && cmake .. && make'",
            "problemMatcher": []
        }
    ]
}